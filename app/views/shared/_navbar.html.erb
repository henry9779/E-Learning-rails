<nav class='navbar navbar-expand-lg navbar-light bg-light'>
  <div class='container-fluid'>
    <%= link_to t('e_learning'), root_path, class: 'navbar-brand'%>
    <div class='navbar-nav'>
      <% if Current.user %>
        <%= link_to t('my_courses'), my_courses_path, { class: 'nav-link' } %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= t('language') %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to t('traditional_chinese'), {locale: 'zh-TW'}, {class: 'dropdown-item'} %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to t('english'), {locale: 'en'}, {class: 'dropdown-item'} %></li>
          </ul>
        </li>
      </ul>
    </div>

    <div class='navbar-nav'>
      <% if Current.user %>
        <span class='navbar-text fw-bold'><%= t('hello', name: Current.user.last_name) %></span>
        <% if Current.user.admin %>
          <%= link_to t('admin_page'), admin_root_path, class: 'nav-link' %>
        <% end %>
        <%= link_to t('log_out'), log_out_path, method: :delete, class: 'nav-link' %>
      <% else %>
        <%= link_to t('sign_up'), sign_up_path, class: 'nav-link' %>
        <%= link_to t('log_in'), log_in_path, class: 'nav-link' %>
      <% end %>
    </div>
  </div>
</nav>
